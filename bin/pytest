#!/usr/bin/env ruby

current_dir = Dir.pwd
command = <<~COMMAND
  docker run \
  --network cic \
  -v #{current_dir}:/mnt/workdir \
  -w /mnt/workdir \
  lvlup/ci_course \
  pytest #{ARGV.join(' ')}
COMMAND
exit 1 unless system(command)
