#!/usr/bin/env ruby
# frozen_string_literal: true

current_dir = Dir.pwd
command = <<~COMMAND
  docker run \
  --network bridge \
  -v #{current_dir}:/mnt/workdir \
  -w /mnt/workdir \
  lvlup/ci_course \
  ansible #{ARGV.join(' ')}
COMMAND
system(command)
